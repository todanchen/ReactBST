import React from "react";

function SplitLineComponent() {
  return (
    <div
      className="splitLine"
      style={{
        height: "0.1px",
        borderTop: "1px solid #ddd",
        padding: "1rem",
        textAlign: "center",
      }}
    ></div>
  );
}

export default SplitLineComponent;
